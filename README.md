#Mongodb , #Render , #Vercel - himakauthpala14@gmail.com - Himz@2004 - Google

#github - it24103099@my.sliit.lk - Himz@123#

Git clone : repo link ,
git status , 
git add .  ,
git commit -m “Initialization ” , 
git push origin , 
All codes go to github , 
After code update start again from git add .  ,
npm install , .gitignore (node_modules) - frontend backend
then npm run dev

frontend
discount:{
      type:Number,
      default:0,
      min:[0,"Discount cannot be negative"],
      max:[100,"Discount cannot be more than 100"]
    }

backend
<label>Discount (%)</label>
      <input name="discount" value={formData.discount} onChange={handleChange} step="0.01" type="number" min="0" max="100" />

First Login to render , then click +new (web service) , select repositpry from coorect github , root dire - backend , backend $ npm install , backend $ npm run dev

Then Login to Vercel add new ,  project 

